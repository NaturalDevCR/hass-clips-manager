# The UI build stage installs its own dependencies and builds inside the image.
# A host node_modules would overwrite them with binaries for the wrong platform,
# and both directories only bloat the context the Supervisor uploads.
ui/node_modules
ui/dist
