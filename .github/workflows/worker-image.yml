name: Worker image

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - build_arch: amd64
            platform: linux/amd64
          - build_arch: aarch64
            platform: linux/arm64
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Compute lowercase image repository
        run: echo "IMAGE_REPOSITORY=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/cinema-collections-worker" >> "$GITHUB_ENV"
      - uses: docker/build-push-action@v6
        with:
          context: app
          file: app/Dockerfile
          platforms: ${{ matrix.platform }}
          build-args: BUILD_ARCH=${{ matrix.build_arch }}
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ env.IMAGE_REPOSITORY }}:${{ matrix.build_arch }}
            ${{ env.IMAGE_REPOSITORY }}:${{ github.sha }}-${{ matrix.build_arch }}
          labels: |
            org.opencontainers.image.source=${{ github.repositoryUrl }}
            org.opencontainers.image.revision=${{ github.sha }}